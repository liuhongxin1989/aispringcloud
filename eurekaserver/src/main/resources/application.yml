server:
  port: 8761

eureka:
  client:
    register-with-eureka: false #是否将当前Eureka Server服务作为客户端进行注册
    fetch-registry: false #是否获取其他的 Eureka Server 服务的数据
    service-url:
      defalutZone: http://localhost:8761/eureka/ #注册中心的访问地址